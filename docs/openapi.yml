---
# ShigotoForm OpenAPI Specification
#
# This application is a fully client-side PWA.
# There is no backend API and no REST endpoints.
# No external network communication occurs; all data is stored locally in IndexedDB.
#
# No applicable API endpoints.
#
# Internal data operations (IndexedDB) are out of scope for OpenAPI.
# For internal API details, see docs/analyzed/databases/schema.md.

openapi: "3.0.3"
info:
  title: ShigotoForm
  version: "0.0.5 alpha"
  description: |
    ShigotoForm is a fully client-side PWA with no public HTTP API.
    This file is kept as a placeholder.
  contact: {}
  license:
    name: "see repository"

paths: {}
# Created at commit: 324ff72
